name: asistencias_movil
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.4
  http: ^0.13.5
  flutter_secure_storage: ^8.0.0
  cupertino_icons: ^1.0.6
  table_calendar: ^3.0.1
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/utils/
    - assets/utils/home.png